---
/**
 * DemoSection - 데모 비디오 영역
 * SaaS 스타일 와이드 비디오 플레이스홀더
 */
---

<div class="demo-section">
  <div class="demo-video">
    <!-- 실제 배포 시 교체: <img src="/demo.gif" alt="PDFnMD 사용 데모" /> -->
    <span class="demo-placeholder">demo.gif</span>
    <span class="demo-caption">PDF 업로드 → 변환 → Notion 붙여넣기 GIF</span>
  </div>
</div>

<style>
  .demo-section {
    margin: 56px 0 0;
  }

  .demo-video {
    width: 100%;
    aspect-ratio: 16 / 9.5;
    background: var(--color-gray-100);
    border-radius: 12px;
    border: 1px solid var(--color-gray-300);
    box-shadow: 0 8px 40px rgba(0, 0, 0, 0.08), 0 2px 8px rgba(0, 0, 0, 0.04);
    display: flex;
    flex-direction: column;
    align-items: center;
    justify-content: center;
    gap: 12px;
    color: var(--color-gray-500);
    overflow: hidden;
    position: relative;
  }

  .demo-placeholder {
    font-size: 28px;
    color: var(--color-gray-300);
    font-weight: 300;
  }

  .demo-caption {
    font-size: 13px;
  }
</style>
