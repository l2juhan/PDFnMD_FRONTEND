---
/**
 * Features - 특징 섹션
 * 2×2 그리드
 */
const features = [
  {
    title: '이미지 자동 추출',
    description: 'PDF 내 이미지를 클라우드에 저장하고 마크다운에 URL로 포함합니다.'
  },
  {
    title: 'Notion 최적화',
    description: 'GFM 형식으로 변환해서 Notion에 붙여넣으면 서식이 그대로 유지됩니다.'
  },
  {
    title: '24시간 보관',
    description: '변환된 이미지는 24시간 동안 클라우드에 보관됩니다. 그 안에 붙여넣으세요.'
  },
  {
    title: '무료',
    description: '회원가입 없이 바로 사용할 수 있습니다. 비용은 없습니다.'
  }
];
---

<section class="section" id="features">
  <p class="section-label">특징</p>
  <h2 class="section-heading">왜 PDFnMD인가</h2>

  <div class="features">
    {features.map((feature) => (
      <div class="feature">
        <h3>{feature.title}</h3>
        <p>{feature.description}</p>
      </div>
    ))}
  </div>
</section>

<style>
  .section {
    padding: 64px 0;
  }

  .section + .section {
    border-top: 1px solid var(--color-gray-300);
  }

  .section-label {
    font-size: 12px;
    text-transform: uppercase;
    letter-spacing: 0.1em;
    color: var(--color-gray-500);
    margin-bottom: 12px;
  }

  .section-heading {
    font-family: 'Newsreader', Georgia, serif;
    font-size: 28px;
    font-weight: 400;
    letter-spacing: -0.02em;
    margin-bottom: 32px;
  }

  .features {
    display: grid;
    grid-template-columns: repeat(2, 1fr);
    gap: 24px;
  }

  .feature {
    padding: 20px;
    border: 1px solid var(--color-gray-300);
    border-radius: 6px;
  }

  .feature h3 {
    font-size: 15px;
    font-weight: 600;
    margin-bottom: 6px;
  }

  .feature p {
    font-size: 14px;
    color: var(--color-gray-700);
    line-height: 1.5;
  }

  @media (max-width: 640px) {
    .features {
      grid-template-columns: 1fr;
    }
  }
</style>
